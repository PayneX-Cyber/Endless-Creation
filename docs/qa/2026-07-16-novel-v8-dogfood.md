# 小说 v8 全链路狗粮 QA

日期：2026-07-16
分支：`codex/novel-v8-dogfood-qa`
基线：`main`（已包含卷结构与场景结构归档）

## 结论

核心写作链路通过。测试使用独立 Electron profile 和真实界面操作，未读取或修改用户真实 API 配置。测试过程中发现并修复 1 个导入阻断：普通正文句子以“第一章”开头时会被误判为章节标题。

## 覆盖与证据

| 能力 | 结果 | 证据 |
| --- | --- | --- |
| 长篇导入 | 通过 | 序言 + 12 章导入为 13 章，1,108 字；“第一章结束时，…”保留在正文 |
| 卷结构 | 通过 | 创建“第一卷 雾港”“第二卷 深潮”，章节分配为 7 + 6 |
| 场景写作 | 通过 | 新建“码头追踪”，编辑标题、大纲与多段正文 |
| 撤销隔离 | 通过 | 第二场景撤销临时输入后，切回第一场景正文不受影响 |
| AI 与版本 | 通过 | AI 续写生成版本 1，确认写回当前场景，历史版本入口可用 |
| 跨章搜索 | 通过 | 搜索第二场景正文，结果定位到第 1 章场景 2 并打开正确正文 |
| Markdown 导出 | 通过 | 含聚合正文和 AI 写回，不含场景标题 |
| Word 分镜本 | 通过 | 含聚合正文和 AI 写回，不含场景标题 |
| ZIP 离线包 | 通过 | 成功生成非空 ZIP |
| 重启恢复 | 通过 | 两卷、场景标题/正文、AI 版本均恢复 |
| 视觉与溢出 | 通过 | Electron 1266×754；工作台无水平页面溢出 |

截图与导出捕获位于本地临时目录 `.codex/qa-v8-artifacts/`，不提交仓库。

## 修复

`parseImportedManuscript` 不再把带句末标点或过长的普通“第 N 章…”正文行识别为标题；Markdown 标题仍明确接受，大纲解析行为不变。模块自检覆盖该回归。

## 未纳入本轮 GUI

人物关系图谱和情感曲线未在本轮隔离 GUI 中重新执行。它们没有被本次修复触及，继续由现有模块自检、TypeScript 构建和 OpenSpec 验证覆盖。

## 最终验证

- `npm.cmd run build`
- 文本完整性扫描
- `openspec validate --all --strict`
